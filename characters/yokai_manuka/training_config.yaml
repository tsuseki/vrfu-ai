---
job: extension
config:
  name: "yokai_manuka"
  process:
    - type: 'sd_trainer'
      training_folder: "loras"
      device: cuda:0
      trigger_word: "yokai manuka"

      network:
        type: "lora"
        linear: 32
        linear_alpha: 16

      save:
        dtype: float16
        save_every: 250
        max_step_saves_to_keep: 4
        push_to_hub: false

      datasets:
        - folder_path: "characters/yokai_manuka/training"
          caption_ext: "txt"
          caption_dropout_rate: 0.05
          shuffle_tokens: false
          cache_latents_to_disk: true
          resolution: [ 512, 768, 1024 ]

      train:
        batch_size: 1
        steps: 2000
        gradient_accumulation_steps: 1
        train_unet: true
        train_text_encoder: true
        gradient_checkpointing: true
        noise_scheduler: "ddpm"
        optimizer: "adamw8bit"
        lr: 1e-4
        ema_config:
          use_ema: true
          ema_decay: 0.99
        dtype: bf16

      model:
        name_or_path: "checkpoints/waiIllustriousSDXL_v170.safetensors"
        is_flux: false
        arch: sdxl

meta:
  name: "yokai_manuka"
  version: '1.0'
